<!-- ================================= -->
<!-- DoctorChatPage.xaml (DOCTOR SIDE) -->
<!-- ================================= -->

<Page
    x:Class="ChatMEDICAL.Views.DoctorChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F4FAFF">

        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="90"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border
            Grid.Row="0"
            Background="#DFF4FF"
            Padding="30">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button -->
                <Button
                    Content="← Back"
                    Width="100"
                    Height="40"
                    Background="#BFE9FF"
                    Foreground="#123B5D"
                    FontWeight="Bold"
                    Click="Back_Click"/>

                <!-- Patient Info -->
                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="6">

                    <TextBlock
                        x:Name="PatientNameText"
                        Text="Patient Name"
                        FontSize="26"
                        FontWeight="Bold"
                        Foreground="#123B5D"
                        HorizontalAlignment="Center"/>

                    <Border
                        Background="#E8FFF0"
                        CornerRadius="12"
                        Padding="12,4"
                        HorizontalAlignment="Center">

                        <TextBlock
                            Text="🟢 Active Consultation"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Foreground="#0F8A43"
                            HorizontalAlignment="Center"/>
                    </Border>

                </StackPanel>

                <!-- Icon -->
                <TextBlock
                    Grid.Column="2"
                    Text="🩺"
                    FontSize="30"
                    VerticalAlignment="Center"/>

            </Grid>
        </Border>

        <!-- CHAT AREA -->
        <ScrollViewer Grid.Row="1">

            <StackPanel
                x:Name="MessagesPanel"
                Padding="30"
                Spacing="18">

            </StackPanel>

        </ScrollViewer>

        <!-- MESSAGE INPUT -->
        <Border
            Grid.Row="2"
            Background="White"
            Padding="20"
            BorderBrush="#DCEEFF"
            BorderThickness="1">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox
                    x:Name="MessageBox"
                    PlaceholderText="Write message to patient..."
                    Height="50"
                    FontSize="15"
                    VerticalAlignment="Center"/>

                <Button
                    Grid.Column="1"
                    Content="Send"
                    Width="120"
                    Height="50"
                    Margin="15,0,0,0"
                    Background="#BFE9FF"
                    Foreground="#123B5D"
                    FontWeight="Bold"
                    Click="Send_Click"/>

            </Grid>
        </Border>

    </Grid>
</Page>